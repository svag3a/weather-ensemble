services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - weather_data:/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/weather.db
    restart: unless-stopped

volumes:
  weather_data:
